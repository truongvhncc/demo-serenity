#-----Database-----
gtpSampleServiceDatabaseUrl=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_PREFIX}
gtpSampleServiceDatabaseUsername=${DB_PREFIX}
gtpSampleServiceDatabasePassword=${DB_PREFIX}
#-----Common-----
language=${LANGUAGE}
waitIntervalForUiInMillisecond=1000
waitForUiInMillisecond=90000


#-----Text for search (aliasMapping)-----
${search-variable}=red