headless.mode = false
serenity.take.screenshots = FOR_FAILURES
#serenity.take.screenshots = AFTER_EACH_STEP
enable.markdown="story,narrative,scenario,step"
serenity.browser.maximized=true
webdriver.driver=provided
# webdriver.provided.type=mydriver
webdriver.provided.mydriver=com.fortna.autofx.common.models.WebDriver
# serenity.driver.capabilities=mydriver
webdriver.chrome.driver="src/test/resources/chromedriver.exe"
serenity.full.page.screenshot.strategy = true
chrome.switches = --incognito;--ignore-certificate-errors;--disable-popup-blocking;--disable-default-apps;--disable-extensions-file-access-check;--disable-infobars;--disable-gpu

webdriver {
    driver = provided
    chrome {
#        switches = [
#            "--incognito",
#            "--disable-features=TrustedTypes",
#        ]
    }
    chromedriver.version = "LATEST"  # Ensure the correct chromedriver version is picked
}